{
  "name": "160finalproject",
  "version": "1.0.0",
  "description": "Install dependencies: ``` pip3 install -r requirements.txt ``` Start the server: ``` python3 -m uvicorn main:app --reload ``` Open the browser and navigate to `http://127.0.0.1:8000`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
